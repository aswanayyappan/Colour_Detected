
  # Convert to React app

  This is a code bundle for Convert to React app. The original project is available at https://www.figma.com/design/bZXIkMw4WillrJkeiua3Rm/Convert-to-React-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  